import React from 'react';
import './style.scss';

function Descr() {
	return (
		<div className="descr">
			<h3 className="descr-title">Правила викторины:</h3>
			<p className="descr-text">
				Сначала послушайте пение птицы, используя плеер,
				а затем выберите один из вариантов
			</p>
		</div>
	)
}

export default Descr